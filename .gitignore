# Dependencies
node_modules/

# Builds
dist/
.vite/

# Env / secrets
.env
.env.*
!.env.example

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Misc
*.pem
.cache/

# Claude Code maintainer-only context (workspace @-imports etc.)
CLAUDE.local.md

# Maintainer-only strategic / planning docs (per OSS repo policy)
_private/

# Local-only screen captures: raw pre-trim sources AND the trimmed
# MP4s used in the README. README demo videos are uploaded to GitHub
# user-attachments (CDN) and referenced by URL, not committed — see
# the `<video>` tag in README.md for the canonical link.
screenshots/*-raw.*
screenshots/*.mp4
